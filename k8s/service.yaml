apiVersion: v1
kind: Service
metadata:
  name: rag-video-service
  labels:
    app: rag-video
spec:
  type: LoadBalancer
  selector:
    app: rag-video
  ports:
    - name: http
      port: 80
      targetPort: 8080
